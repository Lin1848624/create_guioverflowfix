# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，更新日志格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.0.1] - 2026-09-10

本版本只调整模组元数据与文档，Mixin 注入逻辑与 1.0.0 完全一致。在机械动力 6.0.8 上，游戏内表现与 1.0.0 没有区别。

### 修复

- 修正模组在机械动力 6.0.9 及更高版本上被拒绝加载的问题：机械动力的依赖范围由 `[6.0.8]`（仅 6.0.8）改为 `[6.0.8,)`（6.0.8 及以上）。
- 修正 README 与实际功能不符的问题：原 README 描述的是设计稿里的方案（固定宽度数值条、比例坐标映射、K/M/B 紧凑显示），这些功能并未实现。

### 新增

- 新增 `LICENSE` 文件（MIT 协议）。此前只在 `mods.toml` 中声明了 `license="MIT"`，仓库内没有协议原文。

### 变更

- 重写 `README.md`：按实际实现说明修复方式（拦截 `FilteringBehaviour#createBoard()` 中的 `ItemStack#getMaxStackSize()` 并把数值收拢到 64），并补充兼容性、安装步骤、已知限制与构建说明。
- `mods.toml` 的描述补充测试范围（仅在机械动力 6.0.8 上测试，其他版本可能有效也可能无效）与计数过滤上限为 64 的说明。
- 设计文档开头补充实现状态说明，注明文档中的方案 A 未实现，实际采用的是收拢最大值的思路。
- `gradle.properties` 中失效的本机 JDK 路径改为 `D:/App/jdk-17`。

### 说明

- 使用机械动力 6.0.8 的玩家不需要为本版本更新；使用更高版本机械动力的玩家需要更新到 1.0.1 才能加载本模组。
- 计数过滤的可设置最大值仍为 64，该限制与 1.0.0 相同。

## [1.0.0] - 2026-08-28

### 新增

- 首个版本。通过 Mixin 拦截 `FilteringBehaviour#createBoard()` 中对 `ItemStack#getMaxStackSize()` 的调用，把计数过滤的最大值收拢到 64，修复安装 Bigger Stacks、Stacc 等提高物品堆叠上限的模组后，机械动力计数过滤界面随堆叠数膨胀、卡死或崩溃的问题。
- 支持 Minecraft 1.20.1、Forge 47 及以上，需要 Java 17。
